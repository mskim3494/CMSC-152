/* Min Su Kim, mskim3494 */
/* CS152, Winter 2015 */
/* Homework 3 */

#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include <string.h>
#include "hw3.h"

int main(int argc, char *argv[])
{
    int alen = atoi(argv[1]);
    int i=0;
    int* a=malloc(alen*sizeof(int));
    for(i=alen; i>0; i--){
        scanf("%d", &a[i-1]);
    } positives(a, &alen);
    free(a);
    return 0;
}